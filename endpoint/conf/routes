# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /categories/list    controllers.CategoryController.list
GET     /products/list  controllers.ProductController.list(category: Option[String])

# Map static resources from2dd the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
